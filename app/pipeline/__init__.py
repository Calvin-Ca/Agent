"""Layer 2: Perception — document processing, OCR, text extraction and chunking.

Processors extract content from different file types (PDF, image, text).
Each processor implements the DocumentProcessor protocol.
"""
